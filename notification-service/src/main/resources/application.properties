spring.config.import=optional:classpath:.env
spring.application.name=notification-service
# App Settings
server.port=${SERVER_PORT:8084}

# Database Settings
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/notification-service}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# PostgreSQL Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Kafka Settings
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.consumer.group-id=${spring.application.name}
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer

# Reactive call target for enriching notifications with user details
user-service.base-url=${USER_SERVICE_URL:http://localhost:8081}

# Healthcheck endpoint
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
